version: "3.8"

services:
  peerly-core-database:
    image: postgres:latest
    ports:
      - 54321:5432
    environment:
      POSTGRES_USER: peerly-core-user
      POSTGRES_PASSWORD: pwd
      POSTGRES_DB: peerly-core
      POSTGRES_HOST_AUTH_METHOD: trust
    command: -c log_statement=all
